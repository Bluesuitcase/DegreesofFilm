# Copy this file to .env (same folder) and fill in your key.
# curation/.env is gitignored — the key never leaves your machine.
# Get a key at https://www.themoviedb.org/settings/api (use the v3 "API Key").
TMDB_API_KEY=your_tmdb_v3_api_key_here
